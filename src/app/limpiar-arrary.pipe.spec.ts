import { LimpiarArraryPipe } from './limpiar-arrary.pipe';

describe('LimpiarArraryPipe', () => {
  it('create an instance', () => {
    const pipe = new LimpiarArraryPipe();
    expect(pipe).toBeTruthy();
  });
});
